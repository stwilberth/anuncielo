<div>
    <div class="preview-note">Facebook Cover Preview (820 x 360px)</div>
    <div class="cover-container">
        <div class="safe-zone"></div>
        
        <div class="background-shapes">
            <div class="shape shape1"></div>
            <div class="shape shape2"></div>
            <div class="shape shape3"></div>
        </div>

        <div class="megaphone">
            <div class="megaphone-horn"></div>
            <div class="megaphone-body"></div>
            <div class="megaphone-handle"></div>
        </div>

        <div class="sound-waves">
            <div class="wave wave1"></div>
            <div class="wave wave2"></div>
            <div class="wave wave3"></div>
            <div class="wave wave4"></div>
            <div class="wave wave5"></div>
        </div>

        <div class="digital-elements">
            <div class="pixel pixel1"></div>
            <div class="pixel pixel2"></div>
            <div class="pixel pixel3"></div>
            <div class="pixel pixel4"></div>
            <div class="pixel pixel5"></div>
        </div>

        <div class="text-content">
            <h1 class="website-name">anuncielo.com</h1>
            <p class="tagline">Diseño Web y Publicidad que Impulsa tu Marca</p>
        </div>
    </div>
</div>
    <style>
        body {
            margin: 0;
            padding: 20px;
            background: #f0f0f0;
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .cover-container {
            width: 820px;
            height: 360px;
            position: relative;
            overflow: hidden;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 35%, #00b4db 100%);
        }

        .background-shapes {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            opacity: 0.1;
        }

        .shape {
            position: absolute;
            border-radius: 50%;
            background: rgba(255,255,255,0.2);
        }

        .shape1 {
            width: 200px;
            height: 200px;
            top: -50px;
            right: -50px;
            animation: float 6s ease-in-out infinite;
        }

        .shape2 {
            width: 150px;
            height: 150px;
            bottom: -30px;
            left: -30px;
            animation: float 8s ease-in-out infinite reverse;
        }

        .shape3 {
            width: 100px;
            height: 100px;
            top: 50%;
            left: 20%;
            animation: float 7s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
        }

        .megaphone {
            position: absolute;
            left: 80px;
            top: 50%;
            transform: translateY(-50%);
            width: 120px;
            height: 120px;
        }

        .megaphone-body {
            width: 60px;
            height: 40px;
            background: linear-gradient(45deg, #ff6b35, #f7931e);
            border-radius: 20px 5px 5px 20px;
            position: absolute;
            top: 50%;
            left: 30px;
            transform: translateY(-50%);
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }

        .megaphone-horn {
            width: 0;
            height: 0;
            border-left: 30px solid #ff6b35;
            border-top: 25px solid transparent;
            border-bottom: 25px solid transparent;
            position: absolute;
            top: 50%;
            left: 0;
            transform: translateY(-50%);
            filter: drop-shadow(0 4px 8px rgba(0,0,0,0.2));
        }

        .megaphone-handle {
            width: 15px;
            height: 30px;
            background: linear-gradient(45deg, #1e3c72, #2a5298);
            border-radius: 8px;
            position: absolute;
            top: 60%;
            left: 70px;
            transform: translateY(-50%);
        }

        .sound-waves {
            position: absolute;
            left: 200px;
            top: 50%;
            transform: translateY(-50%);
            width: 400px;
            height: 200px;
        }

        .wave {
            position: absolute;
            height: 3px;
            border-radius: 2px;
            opacity: 0.8;
            animation: pulse 2s ease-in-out infinite;
        }

        .wave1 {
            width: 80px;
            background: linear-gradient(90deg, #ff6b35, #f7931e);
            top: 80px;
            left: 0;
            animation-delay: 0s;
        }

        .wave2 {
            width: 120px;
            background: linear-gradient(90deg, #f7931e, #ffcd3c);
            top: 100px;
            left: 20px;
            animation-delay: 0.3s;
        }

        .wave3 {
            width: 100px;
            background: linear-gradient(90deg, #ffcd3c, #35a7ff);
            top: 120px;
            left: 10px;
            animation-delay: 0.6s;
        }

        .wave4 {
            width: 140px;
            background: linear-gradient(90deg, #35a7ff, #00b4db);
            top: 60px;
            left: 30px;
            animation-delay: 0.9s;
        }

        .wave5 {
            width: 90px;
            background: linear-gradient(90deg, #00b4db, #0083b0);
            top: 140px;
            left: 25px;
            animation-delay: 1.2s;
        }

        @keyframes pulse {
            0%, 100% { 
                opacity: 0.8;
                transform: scaleX(1);
            }
            50% { 
                opacity: 1;
                transform: scaleX(1.1);
            }
        }

        .digital-elements {
            position: absolute;
            right: 100px;
            top: 50%;
            transform: translateY(-50%);
            width: 200px;
            height: 150px;
            opacity: 0.6;
        }

        .pixel {
            position: absolute;
            width: 8px;
            height: 8px;
            background: #ffcd3c;
            border-radius: 1px;
            animation: twinkle 3s ease-in-out infinite;
        }

        .pixel1 { top: 20px; left: 30px; animation-delay: 0s; }
        .pixel2 { top: 60px; left: 80px; animation-delay: 0.5s; }
        .pixel3 { top: 100px; left: 20px; animation-delay: 1s; }
        .pixel4 { top: 40px; left: 120px; animation-delay: 1.5s; }
        .pixel5 { top: 80px; left: 160px; animation-delay: 2s; }

        @keyframes twinkle {
            0%, 100% { opacity: 0.6; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.3); }
        }

        .text-content {
            position: absolute;
            bottom: 40px;
            left: 80px;
            color: white;
            text-shadow: 0 2px 10px rgba(0,0,0,0.3);
        }

        .website-name {
            font-size: 42px;
            font-weight: bold;
            margin: 0;
            letter-spacing: -1px;
            background: linear-gradient(45deg, #ffcd3c, #ff6b35);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 2px 4px rgba(0,0,0,0.3));
        }

        .tagline {
            font-size: 16px;
            margin: 5px 0 0 0;
            opacity: 0.95;
            font-weight: 300;
            letter-spacing: 0.5px;
        }

        .safe-zone {
            position: absolute;
            top: 24px;
            left: 0;
            right: 0;
            bottom: 48px;
            border: 2px dashed rgba(255,255,255,0.2);
            pointer-events: none;
        }

        .preview-note {
            position: absolute;
            top: -40px;
            left: 0;
            color: #666;
            font-size: 14px;
            font-weight: bold;
        }
    </style>

 