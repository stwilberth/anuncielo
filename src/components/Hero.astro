<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>Aumenta tus ventas con una presencia digital profesional</h1>
      <p class="hero-subtitle">No solo creamos sitios web. Desarrollamos soluciones digitales que generan resultados medibles para tu negocio.</p>
      <div class="hero-cta">
        <a href="/#contacto" class="btn btn-primary">Solicitar consulta gratuita</a>
        <a href="/#planes" class="btn btn-outline">Ver planes</a>
      </div>
    </div>
    <div class="hero-image">
      <img src="/images/hero-image.svg" alt="Aumenta tus ventas con presencia digital" />
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 6rem 0;
    background: linear-gradient(135deg, #f0f4ff 0%, #e6f7ff 100%);
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    background-image: url('/images/pattern.svg');
    opacity: 0.05;
    z-index: 0;
  }

  .hero .container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    position: relative;
    z-index: 1;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .hero h1 {
    font-size: 3rem;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    color: var(--color-dark);
    font-weight: 800;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--color-gray);
    margin-bottom: 2rem;
    max-width: 600px;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    margin-bottom: 3rem;
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    max-width: 600px;
  }

  .stat {
    display: flex;
    flex-direction: column;
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--color-primary);
    line-height: 1;
  }

  .stat-text {
    font-size: 0.875rem;
    color: var(--color-gray);
  }

  .hero-image {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-image img {
    max-width: 100%;
    height: auto;
  }

  @media (min-width: 1024px) {
    .hero .container {
      grid-template-columns: 1fr 1fr;
    }

    .hero-stats {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 4rem 0;
    }

    .hero h1 {
      font-size: 2.5rem;
    }

    .hero-stats {
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
    }

    .stat-number {
      font-size: 2rem;
    }

    .hero-cta {
      flex-direction: column;
    }
  }

  @media (max-width: 640px) {
    .hero-stats {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .stat {
      flex-direction: row;
      align-items: baseline;
      gap: 0.5rem;
    }

    .stat-number {
      font-size: 1.75rem;
    }
  }
</style>